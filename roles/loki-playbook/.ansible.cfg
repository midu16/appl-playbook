[defaults]
inventory = ./inventory
remote_user = root
ask_pass = false
default_verbosity=4
default_debug=true

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false